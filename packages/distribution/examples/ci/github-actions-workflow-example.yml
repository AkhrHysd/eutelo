# GitHub Actions ワークフロー例（参考用）
# 
# このファイルは、Eutelo ドキュメント構造を CI で検証・自動化する際の
# 参考例として提供されています。
# 
# 実際の実装は各プロジェクトの要件に応じて調整してください。
# 本パッケージでは実装コードは含まれていません。

# 例: ドキュメント整合性チェック
# name: Document Validation
# on:
#   push:
#     branches: [main]
#     paths:
#       - 'docs/**/*.md'
# jobs:
#   validate-docs:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Validate document structure
#         run: |
#           # ここにドキュメント検証ロジックを実装
#           # 例: Frontmatter の必須項目チェック
#           # 例: purpose の整合性検証
#           # 例: ファイル命名規則の確認
#       - name: Check document relationships
#         run: |
#           # ここに関係性チェックロジックを実装
#           # 例: parent / related のリンク切れ確認
#           # 例: PRD ↔ BEH の1:1対応確認

# 例: CHANGELOG 自動更新
# name: Update Changelog
# on:
#   push:
#     branches: [main]
# jobs:
#   update-changelog:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Update changelog from docs
#         run: |
#           # ここにCHANGELOG更新ロジックを実装
#           # 例: Frontmatter から変更を抽出
#           # 例: LLM による要約生成
#       - name: Commit changes
#         run: |
#           git config user.name "doc-bot"
#           git config user.email "bot@example.com"
#           git add CHANGELOG.md
#           git commit -m "chore: update changelog [skip ci]" || echo "No changes"
#           git push

# 例: npm パッケージ公開（provenance署名付き）
# name: Publish Package
# on:
#   release:
#     types: [created]
# jobs:
#   publish:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#       id-token: write
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           registry-url: 'https://registry.npmjs.org'
#       - name: Publish with provenance
#         run: |
#           npm publish --provenance --access public
#         env:
#           NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

